{
  "name": "connect-four-full-stack",
  "version": "1.0.0",
  "description": "Connect Four Game - Full Stack (React + Node.js)",
  "private": true,
  "scripts": {
    "backend": "cd backend && npm install && npm start",
    "backend-dev": "cd backend && npm install && npm run dev",
    "frontend": "npm install && npm start",
    "dev": "concurrently \"npm run backend-dev\" \"npm start\"",
    "install-all": "npm install && cd backend && npm install && cd .."
  },
  "keywords": ["connect-four", "game", "react", "node.js", "full-stack"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
